#include <stdio.h>
#include <math.h>

int main(void){
int numParties, numRidings;
scanf("%d %d", &numParties, &numRidings);
int allInfo[numParties][numRidings];
for (int i = 0; i < numParties; i++){
	for (int j = 0; j < numRidings; j++){
		allInfo[i][j] = 0;
	}
}
//now get info
return 0;
}
