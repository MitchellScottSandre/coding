#ifndef POWERS_H //if not defined, use udnerscore instead of period, these macros prevent multiple copies if the header file is included more than once
#define POWERS_H

int square (int num);//name of prototype parameter does not matter
int cube (int num);
int quartic (int num);
int quintic (int num);

#endif 
