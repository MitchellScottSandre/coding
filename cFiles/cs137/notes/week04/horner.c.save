#include <stdio.h>
#include <assert.h>

double horner(double f[], int m, double x){
	

}

int main(void){
double funct1 = {2, 9, 4, 3};
printf("%g\n", horner (funct1, 0, 0 ));
printf("%g\n", horner (funct1, sizeof(f)/sizeof(f[0]), 1));
printf("%g\n", horner (f, sizeof(f)/sizeof(f[0]); -1);

return 0;
}
